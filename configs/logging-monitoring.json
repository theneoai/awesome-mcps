{
  "splunk-searches": {"command": "uvx", "args": ["mcp-server-splunk-searches", "--url", "${SPLUNK_URL}", "--username", "${SPLUNK_USERNAME}", "--password", "${SPLUNK_PASSWORD}"], "env": {"SPLUNK_URL": "https://localhost:8089", "SPLUNK_USERNAME": "admin", "SPLUNK_PASSWORD": "xxx"}},
  "splunk-indexes": {"command": "uvx", "args": ["mcp-server-splunk-indexes", "--url", "${SPLUNK_URL}", "--username", "${SPLUNK_USERNAME}", "--password", "${SPLUNK_PASSWORD}"], "env": {"SPLUNK_URL": "https://localhost:8089", "SPLUNK_USERNAME": "admin", "SPLUNK_PASSWORD": "xxx"}},
  "splunk-saved-searches": {"command": "uvx", "args": ["mcp-server-splunk-saved-searches", "--url", "${SPLUNK_URL}", "--username", "${SPLUNK_USERNAME}", "--password", "${SPLUNK_PASSWORD}"], "env": {"SPLUNK_URL": "https://localhost:8089", "SPLUNK_USERNAME": "admin", "SPLUNK_PASSWORD": "xxx"}},
  "splunk-apps": {"command": "uvx", "args": ["mcp-server-splunk-apps", "--url", "${SPLUNK_URL}", "--username", "${SPLUNK_USERNAME}", "--password", "${SPLUNK_PASSWORD}"], "env": {"SPLUNK_URL": "https://localhost:8089", "SPLUNK_USERNAME": "admin", "SPLUNK_PASSWORD": "xxx"}},
  "sumologic-searches": {"command": "uvx", "args": ["mcp-server-sumologic-searches", "--access-id", "${SUMOLOGIC_ACCESS_ID}", "--access-key", "${SUMOLOGIC_ACCESS_KEY}"], "env": {"SUMOLOGIC_ACCESS_ID": "xxx", "SUMOLOGIC_ACCESS_KEY": "xxx"}},
  "sumologic-collectors": {"command": "uvx", "args": ["mcp-server-sumologic-collectors", "--access-id", "${SUMOLOGIC_ACCESS_ID}", "--access-key", "${SUMOLOGIC_ACCESS_KEY}"], "env": {"SUMOLOGIC_ACCESS_ID": "xxx", "SUMOLOGIC_ACCESS_KEY": "xxx"}},
  "sumologic-sources": {"command": "uvx", "args": ["mcp-server-sumologic-sources", "--access-id", "${SUMOLOGIC_ACCESS_ID}", "--access-key", "${SUMOLOGIC_ACCESS_KEY}"], "env": {"SUMOLOGIC_ACCESS_ID": "xxx", "SUMOLOGIC_ACCESS_KEY": "xxx"}},
  "cloudwatch-logs": {"command": "uvx", "args": ["mcp-server-cloudwatch-logs", "--access-key-id", "${AWS_ACCESS_KEY_ID}", "--secret-access-key", "${AWS_SECRET_ACCESS_KEY}"], "env": {"AWS_ACCESS_KEY_ID": "xxx", "AWS_SECRET_ACCESS_KEY": "xxx"}},
  "cloudwatch-metrics": {"command": "uvx", "args": ["mcp-server-cloudwatch-metrics", "--access-key-id", "${AWS_ACCESS_KEY_ID}", "--secret-access-key", "${AWS_SECRET_ACCESS_KEY}"], "env": {"AWS_ACCESS_KEY_ID": "xxx", "AWS_SECRET_ACCESS_KEY": "xxx"}},
  "cloudwatch-alarms": {"command": "uvx", "args": ["mcp-server-cloudwatch-alarms", "--access-key-id", "${AWS_ACCESS_KEY_ID}", "--secret-access-key", "${AWS_SECRET_ACCESS_KEY}"], "env": {"AWS_ACCESS_KEY_ID": "xxx", "AWS_SECRET_ACCESS_KEY": "xxx"}}
}