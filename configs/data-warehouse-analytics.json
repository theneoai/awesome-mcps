{
  "clickhouse-databases": {"command": "uvx", "args": ["mcp-server-clickhouse-databases", "--host", "${CLICKHOUSE_HOST}", "--port", "${CLICKHOUSE_PORT}", "--username", "${CLICKHOUSE_USERNAME}", "--password", "${CLICKHOUSE_PASSWORD}"], "env": {"CLICKHOUSE_HOST": "localhost", "CLICKHOUSE_PORT": "8123", "CLICKHOUSE_USERNAME": "default", "CLICKHOUSE_PASSWORD": "xxx"}},
  "clickhouse-tables": {"command": "uvx", "args": ["mcp-server-clickhouse-tables", "--host", "${CLICKHOUSE_HOST}", "--port", "${CLICKHOUSE_PORT}", "--username", "${CLICKHOUSE_USERNAME}", "--password", "${CLICKHOUSE_PASSWORD}"], "env": {"CLICKHOUSE_HOST": "localhost", "CLICKHOUSE_PORT": "8123", "CLICKHOUSE_USERNAME": "default", "CLICKHOUSE_PASSWORD": "xxx"}},
  "clickhouse-queries": {"command": "uvx", "args": ["mcp-server-clickhouse-queries", "--host", "${CLICKHOUSE_HOST}", "--port", "${CLICKHOUSE_PORT}", "--username", "${CLICKHOUSE_USERNAME}", "--password", "${CLICKHOUSE_PASSWORD}"], "env": {"CLICKHOUSE_HOST": "localhost", "CLICKHOUSE_PORT": "8123", "CLICKHOUSE_USERNAME": "default", "CLICKHOUSE_PASSWORD": "xxx"}},
  "trino-catalogs": {"command": "uvx", "args": ["mcp-server-trino-catalogs", "--url", "${TRINO_URL}", "--username", "${TRINO_USERNAME}"], "env": {"TRINO_URL": "http://localhost:8080", "TRINO_USERNAME": "xxx"}},
  "trino-schemas": {"command": "uvx", "args": ["mcp-server-trino-schemas", "--url", "${TRINO_URL}", "--username", "${TRINO_USERNAME}"], "env": {"TRINO_URL": "http://localhost:8080", "TRINO_USERNAME": "xxx"}},
  "trino-tables": {"command": "uvx", "args": ["mcp-server-trino-tables", "--url", "${TRINO_URL}", "--username", "${TRINO_USERNAME}"], "env": {"TRINO_URL": "http://localhost:8080", "TRINO_USERNAME": "xxx"}},
  "trino-queries": {"command": "uvx", "args": ["mcp-server-trino-queries", "--url", "${TRINO_URL}", "--username", "${TRINO_USERNAME}"], "env": {"TRINO_URL": "http://localhost:8080", "TRINO_USERNAME": "xxx"}},
  "databricks-workspaces": {"command": "uvx", "args": ["mcp-server-databricks-workspaces", "--host", "${DATABRICKS_HOST}", "--token", "${DATABRICKS_TOKEN}"], "env": {"DATABRICKS_HOST": "xxx.cloud.databricks.com", "DATABRICKS_TOKEN": "xxx"}},
  "databricks-clusters": {"command": "uvx", "args": ["mcp-server-databricks-clusters", "--host", "${DATABRICKS_HOST}", "--token", "${DATABRICKS_TOKEN}"], "env": {"DATABRICKS_HOST": "xxx.cloud.databricks.com", "DATABRICKS_TOKEN": "xxx"}},
  "databricks-notebooks": {"command": "uvx", "args": ["mcp-server-databricks-notebooks", "--host", "${DATABRICKS_HOST}", "--token", "${DATABRICKS_TOKEN}"], "env": {"DATABRICKS_HOST": "xxx.cloud.databricks.com", "DATABRICKS_TOKEN": "xxx"}}
}